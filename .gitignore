*.retry

# ignore inventory except for the example inventory (allow for override)
inventory/*
!inventory/examples/

# ignore group_vars files except for the all.yml (allow for override)
group_vars/
!group_vars/all.yml
